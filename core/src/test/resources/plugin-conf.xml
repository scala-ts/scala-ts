<scalats>
  <compilationRuleSet>
    <includes>
      <include>ScalaParserSpec\.scala</include>
      <include>Transpiler.*</include>
    </includes>

    <excludes>
      <exclude>foo</exclude>
    </excludes>
  </compilationRuleSet>

  <typeRuleSet>
    <!--
      Regular expressions on type full names.
      Can be prefixed with either 'object:' or 'class:' (for class or trait).
    -->
    <includes>
      <include>org\.scalats\.core\..*</include>
    </includes>

    <excludes>
      <exclude>.*Spec</exclude>
      <exclude>ScalaRuntimeFixtures$</exclude>
      <exclude>object:.*ScalaParserResults</exclude>
      <exclude>FamilyMember(2|3)</exclude>
    </excludes>
  </typeRuleSet>

  <settings>
    <emitInterfaces>true</emitInterfaces>
    <emitClasses>false</emitClasses>
    <emitCodecs>true</emitCodecs>

    <optionToNullable>true</optionToNullable>
    <optionToUndefined>false</optionToUndefined>

    <prependIPrefix>false</prependIPrefix>
    <prependEnclosingClassNames>false</prependEnclosingClassNames>

    <typescriptIndent>  </typescriptIndent>

    <fieldNaming>SnakeCase</fieldNaming>
  </settings>
</scalats>
